{
  "name": "@coverage-insight/action",
  "version": "1.0.0",
  "description": "GitHub Action wrapper: reads inputs, compares coverage via @coverage-insight/core, posts the PR comment/check run",
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b && ncc build src/index.ts -o ../../dist --minify"
  },
  "dependencies": {
    "@actions/cache": "^6.0.1",
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "@coverage-insight/core": "1.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/subhashmahimaluri/vite-pr-coverage-insight.git",
    "directory": "packages/action"
  }
}
